# Isaac Lab イメージのビルド用 Docker Compose
# コンテナの起動は sbatch + docker run で行う（slurm/train.sh 参照）
#
# 使い方:
#   docker compose build isaac-lab

services:
  isaac-lab:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    image: isaac-lab:2.3.2
